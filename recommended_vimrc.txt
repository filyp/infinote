call plug#begin('~/.vim/plugged')
Plug 'ggandor/leap.nvim'
" Plug 'ggandor/leap-spooky.nvim'
Plug 'madox2/vim-ai'
Plug 'MattesGroeger/vim-bookmarks'
Plug 'ixru/nvim-markdown'
call plug#end()

let g:bookmark_save_per_working_dir = 1
let g:bookmark_auto_save = 1


nnoremap ,j <Plug>(leap-from-window)

" to free up <C-i> for jump forward (Ctrl+i is the same keycode as Tab)
nnoremap ,f <Plug>Markdown_Fold


""""""""""""""""""""""""""""
" optional

" " jump around with Ctrl+o and Ctrl+i

" " autowrite:
" autocmd TextChanged,TextChangedI <buffer> silent write

" " disable enter mapping
" map <Plug> <Plug>Markdown_FollowLink